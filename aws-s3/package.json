{
  "name": "aws-s3-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install --workspace=client & npm install --workspace=client",
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server"
  }
}
